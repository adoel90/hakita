const ConvertToComa = (string) => {
    if(string !== null ){
        if(string !== undefined){

            parseFloat(str.replace(',','.').replace(' ',''))
            
        }
    }
};

export default ConvertToComa;

//*Google : change point to coma in javascript in numeral
